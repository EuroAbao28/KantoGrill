export const host = "http://localhost:5000";

export const loginRoute = `${host}/api/admin/login`;

export const productRoute = `${host}/api/products`;

export const transactionRoute = `${host}/api/transactions`;
